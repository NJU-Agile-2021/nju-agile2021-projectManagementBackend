server:
  port: 8088

#pagehelper分页插件配置
pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql

spring:
  profiles:
    active: dev
mybatis:
  mapper-locations: classpath:Mapper/*.xml
  check-config-location: true
  type-aliases-package: com.nju.projectManagement.DO